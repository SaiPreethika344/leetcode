def move_zeros(nums):
    result = []
    for num in nums:
        if num != 0:
            result.append(num)
    result += [0] * (len(nums) - len(result))
    return result

print(move_zeros([0, 1, 0, 3, 12]))
print(move_zeros([0, 0, 1]))
